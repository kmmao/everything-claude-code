---
description: Auto-detect project tech stack and generate a .claude/ecc-install.json for selective ECC installation. Reads project files, maps to ECC modules, confirms with user, writes config, and optionally installs.
---

Invoke the `ecc-project-config` skill to initialize a project-scoped ECC configuration.
